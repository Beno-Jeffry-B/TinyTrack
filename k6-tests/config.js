export const CONFIG = {
  // Production Base URL
  BASE_URL: 'https://tinytrack-ydvz.onrender.com',

  // A valid short code that already exists in your database (to test redirects)
  TEST_SHORT_CODE: 'test002',

  // A valid JWT token (to test protected API routes like /api/url)
  // REPLACE this with a real token from your browser's localStorage for api-test.js
  TEST_TOKEN: 'YOUR_VALID_JWT_TOKEN',

  // A valid URL ID to test analytics (e.g. "1")
  TEST_URL_ID: '1'
};
